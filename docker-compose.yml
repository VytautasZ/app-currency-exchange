services:
  currencyexchange-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: currencyexchange-db
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Developer"
      MSSQL_SA_PASSWORD: "Str0ng!Passw0rd#2026"
    ports:
      - "${MSSQL_PORT:-1433}:1433"
    volumes:
      - currencyexchange-db-data:/var/opt/mssql
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$$MSSQL_SA_PASSWORD\" -C -Q 'SELECT 1' || exit 1"
        ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: unless-stopped

volumes:
  currencyexchange-db-data:
